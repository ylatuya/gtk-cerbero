import os
from cerbero.config import Architecture, Platform

path = os.path.dirname(os.path.abspath(__file__))
external_recipes = {'gtk': (os.path.join(path, 'recipes'), 1)}
external_packages = {'gtk': (os.path.join(path, 'packages'), 1)}

home_dir = os.path.expanduser('~/gtk-build')

# Disable giobject-introspection in cross-compilation
if platform == target_platform:
    variants=['gi']
